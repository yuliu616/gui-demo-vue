import { message } from './message';
import { word } from './word';
import { error } from './error';
import { People } from './People';

const bundle = {
  message,
  word,
  error,
  model: {
    People,
  },
};

export { bundle }
